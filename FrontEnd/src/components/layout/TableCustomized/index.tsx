import React from 'react';
import { Container } from './styles';

const TableCustomized: React.FC = ({ children }) => {
  return <Container>{children}</Container>;
};

export default TableCustomized;
